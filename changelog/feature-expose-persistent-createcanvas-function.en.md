# Persistent canvas integration

## Create persistent canvases through the UI gateway

Integrated browser components can now create a persistent canvas through the same Whiteboard UI gateway that provides disposable canvases.
